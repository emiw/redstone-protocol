/* (c) 2016 EMIW, LLC. emiw.xyz/license */
import test from 'ava';

test('noop', t => t.ok(true));

/*
import * as index from '..';
import createParser from '../dist/Parser';
import { encode, decode } from '../dist/parse';

test('createParser', t => {
  t.is(index.createParser, createParser);
});

test('encode/decode', t => {
  t.is(index.encode, encode);
  t.is(index.decode, decode);
});
*/
